---
name: create-preset
description: Creates Pre-Alert Preset and Full Screen Alert Preset
---

create a new pre-alert preset based on the exact colors in the screenshot. give it the name that is passed into this skill.

then create a full screen alert based on the same color scheme and give the preset the same name, but append " FS" to the end of the string.